Object.assign(app, (function () {
    var actionTypes = {
        START: 'START',
        ROLL: 'ROLL',
        ADD_PLAYER: 'ADD_PLAYER'
    };

    return {
        actionTypes: actionTypes
    }
})());
